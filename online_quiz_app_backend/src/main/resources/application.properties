spring.application.name=online_quiz_app
spring.datasource.url=jdbc:mysql://localhost:3306/online_quiz_db?useSSL=false&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=RJP279
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# JWT settings (used later if you wire real JWT)
app.jwtSecret=ReplaceThisSecretWithAStrongOne
app.jwtExpirationMs=86400000

server.port=8080

